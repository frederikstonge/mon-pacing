import 'package:flutter/material.dart';

import 'pacings_view.dart';

class PacingsPage extends StatelessWidget {
  const PacingsPage({super.key});
  @override
  Widget build(BuildContext context) {
    return const PacingsView();
  }
}
