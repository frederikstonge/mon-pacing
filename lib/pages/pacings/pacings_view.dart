import 'package:flutter/material.dart';

class PacingsView extends StatelessWidget {
  const PacingsView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text("Pacings");
  }
}
